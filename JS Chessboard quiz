var hashMark = ""

function printHash(num1,num2){for (var x = 0; x < num1; x = x + 1){
hashMark = hashMark + "# ";
} 
for (var y = 0; y < num2; y = y + 1){
if ( y % 2 === 0){
function ReverseString(str) {
   return str.split('').reverse().join('')
}
console.log(ReverseString(hashMark))
}else{console.log(hashMark)}

}
}
printHash(8,8)
